{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege base capability for the main window: core ops + the native open/save file dialog. No fs/shell/http/process — all file IO goes through kith-core in Rust commands. App-defined commands are auto-allowed in Tauri v2 and need no entry.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
